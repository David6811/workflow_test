name: GitHub Actions Demo1
run-name: ${{ github.actor }} i testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:  
    runs-on: self-hosted
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - run: ${{ github.action_path }}/startServer.sh
        shell: bash
